import { Node } from '@xyflow/react';

export const getChildNodePosition = (event: MouseEvent, parentNode?: Node) => {
    // const { domNode } = ;

    // parentNode guard clause

    //const panePosition = screenToFlowPosition
};
